# Plan

1. Record approval, preserved behavior, and DTE traceability.
2. Merge current `main` without changing supported setup paths.
3. Restrict local-template execution to an explicit test build/capability.
4. support Git worktree metadata files and validate their referenced common directory.
5. Align recovery automation, UI controls, and owned UI tests/evidence.
6. Keep capability detection authoritative and the final feature release floor deferred.
7. Run manifest, bootstrap, release-safeguard, UI/recovery, and Tauri checks.
