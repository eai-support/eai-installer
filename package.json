{
  "name": "@enterpriseai/setup",
  "version": "0.1.3",
  "private": true,
  "description": "Cross-platform EAI Setup validation and bootstrap tooling",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/verify-version.mjs && node scripts/verify-manifest.mjs && node scripts/test-bootstrap.mjs && node scripts/test-wizard.mjs",
    "validate": "npm test"
  }
}
