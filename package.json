{
  "name": "@enterpriseai/setup",
  "version": "0.3.10",
  "private": true,
  "description": "Cross-platform EAI Setup validation and bootstrap tooling",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/verify-version.mjs && node scripts/verify-manifest.mjs && node scripts/test-bootstrap.mjs && node scripts/test-wizard.mjs && node scripts/test-scenarios.mjs && node scripts/test-release.mjs",
    "validate": "npm test"
  }
}
